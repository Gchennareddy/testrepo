---
- hosts: vagrant
  become: yes
  tasks:
    - name: install tree on remote server
      yum: 
        name: tree
        state: present
        register: tree_store
      when: inventory_hostname == vagrant
     
    - debug:
        msg: "display value is {{tree_store}}"
      
