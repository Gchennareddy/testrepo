---
- hosts: vagrant
  become: yes
  tasks:
    - name: install tree on remote server
      yum: 
        name: tree
        state: present
        register: tree_store
      when: ansible_user == vagrant
     
    - debug:
        msg: "display value is {{tree_store}}"
      
